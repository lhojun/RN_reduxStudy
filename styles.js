import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    ViewStyle:{
      alignItems : "center",
      justifyContent : "center",
      borderWidth : 5,
      borderColor : "#764abc",
    },
    TextStyle:{
     fontWeight : "bold",
     fontSize : 40,
    }
   });
